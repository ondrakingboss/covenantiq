# Local defaults. Copy to backend/.env and start Uvicorn with --env-file .env.
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
COVENANTIQ_DB_PATH=./app/data/covenantiq.db
PORT=8000
